# Claude Code Restored Source Snapshot

## English

Unofficial source snapshot restored from `source-artifacts/cli.js.map`.

Claimed source map provenance: Claude Code `v2.1.88` associated `cli.js.map`.

Source map SHA-256: `7965012b7a5fc9e09d8d747a04c5c32b94696924536e217f686bb1e7ee70a657`.

This repository is provided for research, analysis, and security review only.

Commercial use is prohibited.

See `LICENSE` for the full terms.

Repository layout:

- `restored-src/` restored sources
- `source-artifacts/cli.js.map` source map used for restoration
- `source-artifacts/source-map-contents.md` file counts and example paths from the restored output
- `source-artifacts/missing-modules.md` unresolved relative import targets found during the scan

## 한국어

이 저장소는 `source-artifacts/cli.js.map`에서 복원한 비공식 소스 스냅샷입니다.

표기된 source map 출처 버전은 Claude Code `v2.1.88`입니다.

Source map SHA-256: `7965012b7a5fc9e09d8d747a04c5c32b94696924536e217f686bb1e7ee70a657`

연구, 분석, 보안 검토 용도로만 제공합니다.

상업적 사용은 금지됩니다.

전체 조건은 `LICENSE`를 확인하십시오.

저장소 구성:

- `restored-src/` 복원된 소스
- `source-artifacts/cli.js.map` 복원에 사용한 source map
- `source-artifacts/source-map-contents.md` 복원 결과의 파일 수와 예시 경로
- `source-artifacts/missing-modules.md` 스캔 중 해소되지 않은 상대 import 대상 목록
